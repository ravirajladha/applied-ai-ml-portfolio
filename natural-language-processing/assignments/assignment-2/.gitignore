# Trained model weights (~234 MB) - regenerate with `python train.py`
models/

# Virtual environment
.venv/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# The small demo/test CSVs in data/ ARE tracked on purpose, so the app and
# the ROUGE evaluation work straight after cloning.

# These three are the graded deliverable, so they are tracked on purpose.
# (The portfolio repo that mirrors this project ignores *.pdf and *.html at its
# root, because course material there is large; these negations override that.)
!Group140.pdf
!Group140.html
!PS-11_Assignment-2.pdf
