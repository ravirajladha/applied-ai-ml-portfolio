# Copy this file to .env and fill in your own values.
# .env is git-ignored — never commit real keys/tokens.

# API keys used by webinar/lab notebooks (leave blank if a notebook doesn't need them)
OPENAI_API_KEY=
HUGGINGFACE_TOKEN=
KAGGLE_USERNAME=
KAGGLE_KEY=

# Google Drive folder ID where heavy PDFs/slide decks are hosted
# (linked per-subject from each resources.md)
DRIVE_RESOURCES_FOLDER_ID=
